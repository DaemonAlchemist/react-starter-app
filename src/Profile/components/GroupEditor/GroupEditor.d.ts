import { NamedNode } from "rdflib";

// What gets passed into the component from the parent as attributes
export declare interface IGroupEditorInputProps {
    group: NamedNode;
}

export type GroupEditorProps = IGroupEditorInputProps;